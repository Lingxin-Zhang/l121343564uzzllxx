# 专利与创新边界

线性码 partitioned LUT encoder 已有专利或已有工作，不能把 block-LUT encoder 本身当作本项目主创新。后续写作时应避免“首次提出线性码 block-LUT 编码器”或“首次提出 BCH LUT encoder”等表述。

本项目必须强调 optical FEC repeated kernel workload、bit-exact backend equivalence 和 workload-aware hybrid backend。block-LUT 只是候选 backend primitive 之一。
